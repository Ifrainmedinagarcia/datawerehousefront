import React from 'react'
import TextField from '@material-ui/core/TextField'

const TitleWelcomeForm = ({title}) => {
    return (
        <h5 className="welcome">{title}</h5>
    )
}

export default TitleWelcomeForm
